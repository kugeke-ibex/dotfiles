# Cursor extensions（1 行 1 ID。先頭 # は無視される場合があるので ID のみの行を推奨）
# 取得: cursor --list-extensions >> config/cursor/extensions.txt
# 復元: grep -v '^#' config/cursor/extensions.txt | sed '/^$/d' | xargs -L1 cursor --install-extension
#
# cursor CLI が PATH に無い場合: Cursor から "Install cursor command" を実行。
#
# settings.json で言及している例（未インストールなら追加）
# golang.go
# hashicorp.terraform
# esbenp.prettier-vscode
